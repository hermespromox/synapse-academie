{
  "name": "synapse-academie",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist/formations dist/entreprises dist/formateurs dist/contact dist/api && cp index.html styles.css script.js robots.txt sitemap.xml favicon.svg og-image.png dist/ && cp formations/index.html dist/formations/ && cp entreprises/index.html dist/entreprises/ && cp formateurs/index.html dist/formateurs/ && cp contact/index.html dist/contact/ && cp api/contact.js dist/api/contact.js && echo DONE"
  }
}